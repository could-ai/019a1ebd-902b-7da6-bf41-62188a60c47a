import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF1A237E); // Deep Blue
  static const Color secondary = Color(0xFF00BCD4); // Cyan
  static const Color lightBackground = Color(0xFFFAFAFA); // White
  static const Color darkBackground = Color(0xFF121212);
  static const Color cardColor = Colors.white;
  static const Color darkCardColor = Color(0xFF1E1E1E);
}

class AppConstants {
  static const double kDefaultPadding = 20.0;
}
